<UserControl x:Class="WpfApp1.App.Views.CreateUserView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:local="clr-namespace:WpfApp1.App.Views"
        mc:Ignorable="d"
        Height="450"
        Width="800"
        Background="#FFF4E7">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <Border Grid.Row="0" Style="{StaticResource CreateUserImage}">
            <Image Grid.Row="0" x:Name="userImage" Width="100" Height="100"/>
        </Border>
        
        <TextBox Grid.Row="1" x:Name="usernameInput"/>
        <TextBox Grid.Row="2" x:Name="imageLinkInput"/>
        
        <StackPanel Grid.Row="3" Orientation="Horizontal" HorizontalAlignment="Center" Margin="10">
            <Button x:Name="createUserButton" Content="Create User"/>
            <Button x:Name="dismissButton" Content="Dismiss"/>
        </StackPanel>
    </Grid>
</UserControl>
